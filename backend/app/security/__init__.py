"""安全模块（M7）。

分层防御，每层各一个文件：
  - authz.py —— 工具授权：谁能调哪个工具（scope 校验，确定性硬边界）；哪些是高危工具（需 HITL）。
  - audit.py —— 审计落库：越权拒绝 / 人工拒绝 / 高危执行 / 注入检出 / PII 脱敏都写 AuditLog，可追溯。
  - guards.py（M7b）—— 注入检测（直接+间接）+ PII 脱敏 + 数据/指令分离信封（输入/输出护栏，概率性防线）。

红线：护栏（guards）是概率性的、能被绕过，只提高攻击成本；真正的硬边界是 authz 的 scope
（确定性）+ M3 的 RLS（数据层）。授权绝不放在 system prompt 里"信任 LLM 自觉"。
"""
